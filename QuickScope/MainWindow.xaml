<Window x:Class="QuickScope.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:QuickScope"
        mc:Ignorable="d"
        Title="QuickScope Settings" Height="250" Width="400"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize">
    
    <StackPanel Margin="20" Background="#F5F5F5">
        <TextBlock Text="QuickScope Options" FontSize="18" FontWeight="Bold" Margin="0,0,0,15"/>

        <CheckBox x:Name="CrosshairCheck" Content="Show Sniper Crosshair Cursor" Margin="0,5" Checked="SettingChanged" Unchecked="SettingChanged"/>
        
        <StackPanel Orientation="Horizontal" Margin="0,5,0,15">
            <CheckBox x:Name="SoundCheck" Content="Play 'Snap' Sound on Capture" VerticalAlignment="Center" Checked="SettingChanged" Unchecked="SettingChanged"/>
            <ComboBox x:Name="SoundDropdown" Width="100" Margin="10,0,0,0" SelectionChanged="SoundDropdown_SelectionChanged"/>
        </StackPanel>

        <TextBlock Text="Selection Border Color (Hex):" Margin="0,5,0,2"/>
        <StackPanel Orientation="Horizontal">
            <TextBox x:Name="ColorHexBox" Width="100" Text="#FFFFFF" TextChanged="ColorHexBox_TextChanged"/>
            <Rectangle x:Name="ColorPreview" Width="20" Height="20" Margin="10,0,0,0" Fill="White" Stroke="Black"/>
        </StackPanel>

        <TextBlock Text="Press CTRL + SHIFT + S to capture." Foreground="Gray" Margin="0,20,0,0" FontSize="11" FontStyle="Italic"/>
    </StackPanel>
</Window>
